# Dream Island — Codex 3D polish, round 2

**STATUS: FINAL (2026-09-11). Start from the tip of `work/dream-island` at or after `40e5856` (phases A–D and polish round 1 all committed).**

Round 1 (`docs/briefs/DREAM-ISLAND-CODEX-POLISH.md`, committed as `ef36809`) closed its nine residuals against their measurables and the map is scope-complete. Round 2 is about the remaining distance to the reference frames — the things a person still sees when the before/after sheet (`art/evidence/dreamisland-v1/polish/eyeball-before-after.png`) sits next to `art/references/dreamisland/heroes/`. Read `DREAM-ISLAND-HANDOFF.md` first (environment, rules, report format), then round 1's §2 collision rule and §3 ceilings — both still apply verbatim, with these updates: **the budget you inherit is worst-tier 93 draws / 151,404 triangles against the 110 / 180,000 gate (17 draws, 28.6k triangles spare)**, the shell gzip has **0.2 KiB** of headroom, and `src/game/game.ts` is at its 2577-line seam ceiling — you may not touch it.

## 1. Gap list first, again

Before changing anything, put `polish/eyeball-before-after.png` beside the reference heroes and write the five biggest remaining gaps you see. Then reconcile them against §2 below; where you disagree with an item, say so and why. That list is the first section of your report.

## 2. Residuals (each names the tile, the defect, and the number that must move)

1. **The sea is still not the reference's sea** — BEACH/REEF day tiles vs `01_beach_straight_day.png`, `06_reef_shallows_day.png`. Round 1 gave it swell and a haze band; it is still a muted mid-blue where the reference is saturated cobalt with a hard cyan foam edge and a visible shallows-to-deep step. Measurable: at the REEF day pose, the deep-sea region's mean chroma (CIE C*ab, or the max−min of RGB as a proxy) must reach ≥ 70% of the reference frame's sea chroma (measure the reference on its own sea region and report both numbers), and the shallows-to-deep boundary must show a luma step ≥ 0.08 over ≤ 12 px. Do it in the water atlas tint and the vertex tint, not by exempting the sea from tone mapping.
2. **The grove is still a row of palms, not a canopy** — GROVE day tile vs `02_palm_grove_day.png`. The reference's fronds cross the road overhead and shade it. Target: crossed frond cards reaching over the road from both verges in the GROVE district (corridor validator must stay at 0 intrusions — fronds ≥ 2 m above the corridor ceiling), and a dappled-shadow decal strip on the road (one card, jungle batch, projected shadow texture from the frond cell, ≤ 0.35 alpha). Measurable: at the GROVE day tile, foliage pixels in the top third of the frame ≥ 40% of columns (round 1 hit 25% at the horizon line); road-region luma standard deviation rises (report before/after) without the mean dropping more than 10%.
3. **Night beacon** — POINT night tile. **RULING 2026-09-11: target withdrawn as written.** Measured after the pass: 4 px / 1.057× at the BEACH pose (tower 624 m away through the shared exponential fog). The 12 px / 4× figure was written without a base capture — exactly the invented-number class this project forbids — and the 4× cannot be met without exempting the lamps from fog, which the render rule prohibits. The beacon is accepted where it is racing-relevant: the lit bore at the POINT approach (night tile, measured in round 1). If a distant beacon is wanted later, the target must be derived from a capture of the lamps alone at 624 m with the fog density that ships, not typed. Round 1 lit the bore interior. From the BEACH straight the tunnel mouth should read as a beacon down the road (§2 of the level brief). Measurable: at the BEACH night pose, the tunnel-mouth region (mask it by rendering the bore lamps alone, like the crossfade instrument masks sky and road) has mean luma ≥ 4× the surrounding drum's and is ≥ 12 px wide at 1280×720.
4. **The strike as a sequence** — `polish/index.html` flipbook. The eased crossfade now steps evenly in luma, but the *sky* content swap (clouds → stars) still reads as a dissolve of two photographs. Target: the night panorama's stars fade in over the last 40% of the ramp only (a second uniform, `starReveal = smoothstep(.6, 1, nightBlend)`, applied as a multiplier on the night sampler's luma above the horizon), so clouds go dark before stars appear. Re-run `crossfade-profile` on a new baseline; T1–T3 must hold on it (T3 is a repeat-check of the new baseline, not a freeze — see round 1's Q&A); report the ten means before/after.
5. **Hero detail passes** — the heroes are proportionally right and texturally flat:
   - Clock tower (`heroes/clock-tower.glb`): the face needs a raised bezel ring casting a shadow, hour ticks as twelve small dark rects on the emissive face cell (no numerals), and the stair treads a visible nosing. ≤ +1,500 triangles.
   - Waterfall cliff: the sheet needs a second, faster scrolling card layer at 60% alpha and a mist card at the plunge pool (jungle-card sheet has no mist; use a soft radial from the emissive lamp cell at low alpha, through the render rule). ≤ +200 triangles, ≤ +1 draw.
   - Sea stacks: add a wave-cut notch band at the waterline (a darker vertex-tint band 1.5–2.5 m above y = 0) so they sit *in* the sea rather than on it.
   - Watchtower: crenellation tops get the moss cell; the two arched windows get the lamp cell at night (∝ nightBlend). ≤ +400 triangles.
   Measurable per hero: a 40 m frame by day and by night, each feature visible; the feral soak still ≤ 110 draws / ≤ 180,000 triangles with the per-hero delta reported.
6. **Kerb stripe by night** — BEACH night tile: the cyan kerb stripe that reads by day disappears at night, and it should be part of the edge cue. Give the kerb stripe an emissive term ∝ nightBlend at a fraction of the foam's (start at 0.4× and calibrate by rendered luma at the pinned pose). Measurable: at the BEACH night pose the kerb stripe's mean luma ≥ 0.25 and ≤ the foam line's.
7. **Signage at speed** — `start-race.png` shows the DREAM ISLAND plate crisp; the in-race gate plates 1–6 are unreadable at chase distance in every tile. The 0.59 m letter cap holds, so the fix is contrast and placement: darker plate background from the signage cell, plates rotated to face the approach by 15°, one plate per gate on the inside of the corner. Measurable: at the gate-1 approach pose (progress 0.115, chase geometry) the plate's digit is ≥ 9 px tall with contrast ≥ 0.4 against its plate.

## 3. Standing rules from round 1 that still bind

No AI image/texture/3D generators (rendering your own geometry is required evidence). Every luma, chroma, draw and triangle number comes from the named instrument (`crossfade-profile`, `race.mjs --tier=feral`, `atlas-quadrant-proof`, `frames.mjs`); calibrate tints by rendered luma at a pinned pose, never by hex. Everything through the shared lighting/fog/tone-mapping. Every new consumer of an atlas cell gets a quadrant-ID proof. `npm run test:code` passes at the end (python3 + Pillow on PATH). Re-shoot the same 17 tiles so before/after is one sheet, and re-render the four hero 40 m frames day and night. Use `grep`, not `rg`. Do not commit.

## 4. Report

The handoff's format, plus: the §1 gap list; per-item before/after frame and the measurable's before/after value; the §3 ceiling table with measured values; the crossfade ten-means table; per-hero draw/triangle delta; open gaps.
