# Post-E follow-ups

The user's measurement verdict is retained as recorded: active-window residuals 22–60 samples, tail residual zero, draws 127/145, pack counts zero residual. The repeated 8.600 ms p95 is a quantised reading and is not a before/after performance comparison. Earlier evidence is unchanged.

## Executed and observed

- [Device comparison](device-pair/index.html): generated a proposed Ascension-native P-12 hero with built-in GPT Image 2 using `hero_prompt_template`; corrected the first output's photoreal treatment. Full prompts and returned artifact IDs are recorded in `art/references/ascension/generation.json`. Service job IDs were not exposed and remain null. Paired the corrected proposal with the unchanged Phase E current-device capture. Two expected views, two posted, zero count residual; SHA-256 commitment in the manifest, answer mapping outside the checkout.
- [Archive recovery](archives/README.md): located and restored the three original environment packages, and executed their existing validation entry points. The recovery record includes rejected copies, hashes, outputs and a reproducible restore script.

## Proposed interpretation and open limits

The device direction uses an olive pressure case, off-white ceramic iris, caged sodium-amber glass and a metal skid base to suggest launch-pad protective hardware. This is a design proposal, not accepted fidelity or a shipped replacement. The generated hero and current runtime card differ in camera framing and background; they cannot establish matched-camera quality or render integration. Generation previews were visible during creation, so the pair is not claimed to be independently source-blind. Its A/B mapping is still withheld.

Gap 4 stays open until the proposed convention is reviewed and, if selected, implemented and validated. The original phase-classification and station-pair keys remain withheld pending the user's answers; none have been scored or revealed. No level acceptance, main merge or next-level work was performed.

Gap 9's requested Stage 1/Stage 2/v1.0 checks now PASS. Seven other historical provenance inputs still cause `validate:assets` to skip; they are listed in the recovery report. No broader archive-completeness claim is made.
