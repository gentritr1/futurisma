# Carry-over implementation and evidence

Executed: player E activation, four physical device cradles, both phase bulkheads, the explicit `demoChain=1` scenario, and egret rise/scatter. Gravity remains disabled; the existing drift bank remains active on gravel and the Mangrove Cut. The production Tideline power simulation supplies Surge, Shield and CHAIN rules.

Observed by `scripts/visual/ascension/powers.mjs`: a real E keypress activated Surge; the separate fresh demo absorbed the trench-exit field at tick 1976, chained at tick 1989, and received 60 / 120 = 0.5 seconds. `chain-live.mp4` preserves the screenshot timestamps; the loop captured 7 frames over 4.148 s against 4.148 × 10 = 41.48 requested samples, residual −34.48. This low-rate live HUD evidence is not a rendering benchmark.

| Full race | Lap times (ms) | Peak total draws | Peak main triangles | Tail p95 (ms) |
|---|---|---:|---:|---:|
| trench | [29975, 28100, 27983] | 124 | 213808 | 9.60 |
| deluge | [33408, 29992, 29867] | 127 | 207152 | 8.60 |
| trench-reduced | [29975, 28100, 27983] | 124 | 213808 | 8.50 |

These numbers come from `race.mjs` and `instrument.mjs`. `executed-results.json`, generated by `report-carry-overs.py`, reconciles calibration, all-recorded, active and tail windows against each measured refresh rate. Every run has zero missed gates, recoveries and browser errors. Trench and reduced-motion lap times match exactly. Peak total draws rise from 119 at 5a2db83 to 127 here, leaving 18 against the Phase C limit of 145.

The full demos now use different tactical power choices: the trench demo saves its Pad Road Surge for the Causeway to keep its fork approach controlled. Do not read their whole-lap difference as isolated route savings. `scripts/measure-ascension-driving.mjs` still measures 3.525 s saved per fork with matched controls and no powers/traffic: surface 42.925 s × 120 = 5151 ticks; trench 32.35 s × 120 = 3882 ticks across three forks.

Clearance was rechecked by `scripts/validate-ascension-events.mjs`: crawler minimum 12.4801 m over 2 × (6 s × 120 Hz + 1 endpoint) = 1442 samples; rocket minimum 12.3781 m over 7.0166667 s × 120 Hz + 1 endpoint = 843 samples. The runtime clock test uses 120 s × 120 Hz = 14400 ticks at each of three render drivers. No route or solid event geometry changed.

The low crawler side card is in `crawler-low/`. It shows tread links from 2.4 m above the asset base, as an explicitly permitted inspection alternative. It does not claim the crawlerway deck no longer occludes the normal road camera.

The corrected frozen station set has 4 × 8 = 32 road samples plus 4 × 2 = 8 board samples; these are discrete poses, not timed windows. All board checks and new road floors pass. `record-road-base.py` ran before `pin-road-floor.py`. The Pad Road patch moves from x=640 to x=500 because the hanging device now covers the original pixels. Its new valid base is 95.2033, floor 80.9228. The files named `device-occluded` preserve the invalid earlier measurement and must not be used as road evidence. Previous blockout, B and C sets remain intact. Different Pad Road pixel regions are not a like-for-like luma comparison.

The live apron fixture visibly retains the road and 249 km/h readout through steam and the new smoke cloud. `steam-speed/reconciliation.json` records 214 decoded frames: recorder 8.0379 s × 30 = 241.137, residual −27.137; container 7.310842 s × 30 = 219.32526, residual −5.32526. Requested capture cadence is not a guaranteed frame rate.

Implemented but not independently visually classified: egret rise on each lap and launch scatter. The live final diagnostics reach bird lap three; the frozen scatter image is an inspection fixture. The route, accepted trench paint and approved focal models remain unchanged.

Validation: `npm run test:code`, build, shared simulation/hardware checks, Ascension power rules, runtime and clearance checks passed. Missing accepted Greenwater archives remain unvalidated. Human device/focal quality, remaining reference work and Phase E reviews remain open; see `OPEN-GAPS.md`. Phase D audio follows this commit.
