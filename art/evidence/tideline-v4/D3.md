# D3 — Salvaged pump hardware and surfaced powers

Executed: new reference triplet and painted metal/signage/lamp atlases; Blender build with independent turbine/iris pivots, caged lamps, bolts, stencils and concrete plinths. `validate-power-kit-v2.mjs` measures 3340 triangles across both devices and checks all embedded painted maps and exported pivots. Existing maps still request their original asset.

Observed: `devices.mjs` renders the actual GLB with the review exposure. `device-comparison.py` pairs it with each hero panel. `devices/independent-review.md` names five shared details for each and passes identity, with simplified mechanical depth and subdued beacons still qualified.

Implemented: suspended cradles with attached hoist cables, opening jaws, sparks and clunk; painted launch strips and glass-covered current cable trays; striped pressure rings, refracting membranes, advance klaxon and absorbed droplets; sodium refund dome, Surge FOV/heat distortion and seeded rival presentation cues. A Shield absorption followed by a held Surge within two seconds adds half a second. Rivals never change pace because of these presentation cues.

Observed interim full race: `race.mjs` in `D3-race/` matched baseline lap times and classification, with no browser errors. No CHAIN fired in that particular demo. Nine approach warnings were recorded with predicted arrival lead greater than two seconds; this is distance/speed evidence, not an audio-perception claim. `validate-tideline-power-chain.mjs` separately exercises the reward boundaries. That initial capture had expensive full-scene transmission; the final build replaces it with framebuffer-copy distortion, and the final budget run supersedes these interim costs.

`validate-tideline-hardware.mjs` checks hoist attachment, collect/reset, paused and reduced motion and seeded rival cues without pace writes. `validate-tideline-runtime.mjs` compares fixed-step outcomes at multiple presentation rates. Visual fog-distance and approach acceptance are delivered with the final evidence package, not inferred from these checks.

Final rendering optimization: `tideline-device-batch.ts` retains the original named pivots and copies changed local vertices/normals into one painted batch per role; the bulb remains independently controlled. `validate-tideline-hardware.mjs` compares every batched animated vertex against its original pivot transform at rest, partial activation and full activation. Distant cradles share static role batches with identical bounds. The gallery's idle device renders remain visually identical; the refreshed five-detail review is in `devices/batch-refresh-review.md`.

The pickup supports use chipped grey metal and heavier suspension wires. Launch paint uses broad distressed cream chevrons pointing in the travel direction, plus the same small SURGE stencil on the deck and plates attached to the guardrails. Flush lamps and cable-tray lamps remain physically recessed. Geometry and brightness are still processed by the D2 rule. The final approach review reports both unaided discovery and focused identity; a target-coordinate prompt must not be mistaken for unaided legibility.
